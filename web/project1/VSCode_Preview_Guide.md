# VSCode Preview 启动指南

## ✅ 已完成的配置

你的项目现在已经配置好了完整的 Preview 启动方式！

### 📦 新增/更新的文件

1. ✅ **package.json** - 添加了 11 种 preview 启动脚本
2. ✅ **webapp/preview.html** - Fiori Tools Preview 页面
3. ✅ **webapp/test/flp.html** - Fiori Launchpad Sandbox
4. ✅ **.vscode/tasks.json** - VSCode 任务配置
5. ✅ **.vscode/launch.json** - VSCode 调试配置
6. ✅ **PREVIEW_MODES.md** - 完整的启动方式文档

---

## 🚀 立即开始使用

### 方法 1：使用 NPM Scripts 面板（最简单）⭐⭐⭐⭐⭐

1. 在 VSCode 中打开项目
2. 在左侧 Explorer 中找到 **NPM SCRIPTS** 面板
3. 展开 **zzabaplint** 项目
4. 找到想要的启动方式，点击右侧的 ▶️ 播放按钮

#### 推荐的启动方式：
- **preview** - 日常开发最常用 🌟
- **preview-adaptation** - UI 设计和适配 🎨
- **preview-standalone** - 快速启动调试 ⚡
- **start** - 完整 FLP 环境测试 🏠

### 方法 2：使用任务菜单

1. 按 `Ctrl+Shift+P`（Windows/Linux）或 `Cmd+Shift+P`（Mac）
2. 输入 "Tasks: Run Task"
3. 选择你需要的任务：
   - `▶️ Preview: 标准预览`
   - `🎨 Preview: UI 适配模式`
   - `⚡ Preview: 独立模式`
   - `🚀 Preview: FLP 模式`
   - 等等...

### 方法 3：使用键盘快捷键

#### 运行默认任务
- Windows/Linux: `Ctrl+Shift+B`
- Mac: `Cmd+Shift+B`

默认任务是：`🏠 Start: FLP 完整模式`

### 方法 4：使用调试功能（F5）

1. 切换到调试视图（左侧图标栏）
2. 从下拉菜单选择配置：
   - `🚀 Launch: Preview` - 标准 Preview
   - `🎨 Launch: Preview with RTA` - 带 RTA 的 Preview
   - `🏠 Launch: FLP` - Fiori Launchpad
   - `⚡ Launch: Standalone` - 独立模式
3. 按 `F5` 启动

### 方法 5：终端命令

在 VSCode 终端中（`` Ctrl+` ``）运行：

```bash
# 最推荐 - 标准 Preview
npm run preview

# UI 适配模式
npm run preview-adaptation

# 独立模式（最快）
npm run preview-standalone

# FLP 模式
npm run preview-flp

# 完整列表见下方
```

---

## 📋 所有可用的 Preview 命令

### Preview 系列（推荐）✨

```bash
npm run preview                    # 标准预览 ⭐⭐⭐⭐⭐
npm run preview-adaptation         # UI 适配（RTA）🎨
npm run preview-standalone         # 独立模式 ⚡
npm run preview-flp                # FLP 预览
npm run preview-variants           # 变体管理
npm run preview-local              # 本地库预览
npm run preview-mock               # Mock 数据预览
```

### Start 系列（传统）

```bash
npm start                          # FLP 完整模式
npm run start-local                # 本地配置
npm run start-noflp                # 无 FLP
npm run start-mock                 # Mock 数据
```

### 构建和部署

```bash
npm run build                      # 标准构建
npm run build-opt                  # 优化构建
npm run deploy                     # 部署到 SAP
npm run deploy-test                # 测试部署
```

### 测试

```bash
npm run unit-test                  # 单元测试
npm run int-test                   # 集成测试
npm run lint                       # 代码检查
npm run ts-typecheck               # 类型检查
```

---

## 🎯 推荐的使用场景

### 场景 1：日常开发
```bash
npm run preview
```
或在 NPM Scripts 面板点击 **preview** ▶️

**特点**：
- ✅ 启动快
- ✅ 包含轻量级 FLP
- ✅ 支持热重载
- ✅ 适合日常开发

### 场景 2：UI 设计和调整
```bash
npm run preview-adaptation
```
或在任务菜单选择 `🎨 Preview: UI 适配模式`

**特点**：
- ✅ 启用 RTA（Runtime Adaptation）
- ✅ 可视化编辑界面
- ✅ 拖拽式调整 UI
- ✅ 实时预览修改

### 场景 3：快速调试
```bash
npm run preview-standalone
```
或在 NPM Scripts 面板点击 **preview-standalone** ▶️

**特点**：
- ✅ 最快启动
- ✅ 无 FLP 外壳
- ✅ 专注应用本身
- ✅ 适合快速验证

### 场景 4：测试 FLP 集成
```bash
npm start
```
或按 `Ctrl+Shift+B` 运行默认任务

**特点**：
- ✅ 完整 FLP 环境
- ✅ 模拟真实 Launchpad
- ✅ 测试磁贴和导航
- ✅ 适合最终测试

### 场景 5：前端开发（无后端）
```bash
npm run preview-mock
```
或在 NPM Scripts 面板点击 **preview-mock** ▶️

**特点**：
- ✅ Mock OData 服务
- ✅ 无需后端连接
- ✅ 前端独立开发
- ✅ 演示和测试

---

## 🎨 VSCode 界面操作指南

### NPM Scripts 面板位置

```
Explorer (资源管理器)
├── 打开的编辑器
├── 项目文件夹
│   └── zzabaplint
└── NPM SCRIPTS ⬅️ 在这里！
    └── zzabaplint
        ├── build
        ├── deploy
        ├── lint
        ├── preview ⭐ ← 点击播放按钮
        ├── preview-adaptation 🎨
        ├── preview-standalone ⚡
        ├── preview-flp
        ├── preview-variants
        ├── preview-local
        ├── preview-mock
        ├── start
        ├── start-local
        ├── start-noflp
        ├── start-mock
        └── ...
```

### 任务快捷操作

**运行任务**：
1. `Ctrl+Shift+P` → "Tasks: Run Task"
2. 选择任务（有图标标识）

**运行默认任务**：
- 直接按 `Ctrl+Shift+B`

**查看正在运行的任务**：
- `Ctrl+Shift+P` → "Tasks: Show Running Tasks"

**终止任务**：
- `Ctrl+Shift+P` → "Tasks: Terminate Task"

### 调试配置使用

1. 点击左侧活动栏的 **Run and Debug** 图标
2. 从下拉菜单选择配置：
   - 🚀 Launch: Preview
   - 🎨 Launch: Preview with RTA
   - 🏠 Launch: FLP
   - ⚡ Launch: Standalone
3. 按 `F5` 启动
4. 应用会自动打开浏览器并附加调试器

---

## 💡 小技巧

### 技巧 1：设置自己的默认任务

编辑 `.vscode/tasks.json`，找到你最常用的任务，修改：

```json
"group": {
  "kind": "build",
  "isDefault": true  // ← 设为 true
}
```

这样按 `Ctrl+Shift+B` 就会运行你的默认任务。

### 技巧 2：创建键盘快捷键

1. `Ctrl+Shift+P` → "Preferences: Open Keyboard Shortcuts (JSON)"
2. 添加：

```json
{
  "key": "ctrl+alt+p",
  "command": "workbench.action.tasks.runTask",
  "args": "▶️ Preview: 标准预览"
}
```

### 技巧 3：在状态栏显示当前任务

VSCode 底部状态栏会显示正在运行的任务，点击可查看输出。

### 技巧 4：同时运行多个任务

可以分别启动不同的任务，每个任务会在独立的终端中运行。

### 技巧 5：自动保存 + 热重载

1. 启用自动保存：`File` → `Auto Save`
2. 运行 preview 任务
3. 修改代码会自动重载浏览器

---

## 🔧 配置说明

### preview.html 页面

位置：`webapp/preview.html`

这是 Fiori Tools 的 Preview 页面，包含：
- ✅ 轻量级 FLP 外壳
- ✅ RTA 支持
- ✅ 快速加载
- ✅ 开发者友好

### test/flp.html 页面

位置：`webapp/test/flp.html`

这是完整的 FLP Sandbox，包含：
- ✅ 完整 Launchpad 功能
- ✅ 磁贴和组
- ✅ 导航和路由
- ✅ 真实环境模拟

### tasks.json 配置

位置：`.vscode/tasks.json`

定义了所有可以在 VSCode 中运行的任务，包含：
- ✅ 20+ 个预配置任务
- ✅ 带图标的任务标签
- ✅ 合理的默认设置
- ✅ 独立的终端面板

### launch.json 配置

位置：`.vscode/launch.json`

定义了调试配置，包含：
- ✅ 4 种启动配置
- ✅ Chrome 调试支持
- ✅ 自动启动服务器
- ✅ 断点调试支持

---

## 📊 对比表：选择合适的启动方式

| 需求 | 推荐命令 | 特点 |
|------|---------|------|
| 日常开发 | `preview` | 快速、轻量、热重载 |
| UI 设计 | `preview-adaptation` | RTA、可视化编辑 |
| 快速调试 | `preview-standalone` | 最快、无外壳 |
| FLP 测试 | `start` 或 `preview-flp` | 完整环境 |
| 前端开发 | `preview-mock` | Mock 数据 |
| 离线开发 | `preview-local` | 本地库 |
| 变体测试 | `preview-variants` | 个性化 |

---

## ⚠️ 常见问题

### Q1: 点击 NPM Scripts 没有反应？

**解决**：
- 确保已安装依赖：`npm install`
- 重启 VSCode
- 检查终端是否已打开

### Q2: 浏览器没有自动打开？

**解决**：
- 手动打开：http://localhost:8080/preview.html
- 检查端口 8080 是否被占用
- 查看终端输出的实际 URL

### Q3: Preview 页面显示空白？

**解决**：
- 检查浏览器控制台错误（F12）
- 确认 manifest.json 中的 ID 正确
- 清除浏览器缓存（Ctrl+Shift+Delete）

### Q4: RTA 模式无法启动？

**解决**：
- 确保 ui5.yaml 中有 fiori-tools-preview 配置
- 检查 @sap/ux-ui5-tooling 是否已安装
- 尝试 `npm install` 重新安装依赖

### Q5: 任务运行后无法停止？

**解决**：
- `Ctrl+Shift+P` → "Tasks: Terminate Task"
- 或直接关闭终端面板
- 或按终端右上角的垃圾桶图标

---

## 🎓 学习资源

- **PREVIEW_MODES.md** - 详细的启动方式说明
- **README.md** - 项目总体文档
- **TROUBLESHOOTING.md** - 问题诊断指南
- [SAP Fiori Tools 文档](https://help.sap.com/docs/SAP_FIORI_tools)
- [VSCode Tasks 文档](https://code.visualstudio.com/docs/editor/tasks)

---

## ✨ 快速参考卡片

```
┌─────────────────────────────────────────────────┐
│  最常用的 3 个命令                              │
├─────────────────────────────────────────────────┤
│  npm run preview              日常开发 ⭐⭐⭐⭐⭐│
│  npm run preview-adaptation   UI 设计  🎨       │
│  npm run preview-standalone   快速调试 ⚡       │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  VSCode 快捷键                                   │
├─────────────────────────────────────────────────┤
│  Ctrl+Shift+B    运行默认任务                    │
│  Ctrl+Shift+P    命令面板 → Tasks: Run Task    │
│  F5              启动调试                        │
│  Ctrl+`          打开/关闭终端                   │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  NPM Scripts 面板位置                            │
├─────────────────────────────────────────────────┤
│  左侧 Explorer → NPM SCRIPTS → 点击 ▶️ 播放     │
└─────────────────────────────────────────────────┘
```

---

## 🎉 开始使用

现在你可以：

1. ✅ 打开 NPM Scripts 面板
2. ✅ 找到 **preview** 并点击播放按钮
3. ✅ 浏览器会自动打开应用
4. ✅ 开始开发！

祝你开发愉快！🚀

---

**提示**：第一次运行会安装依赖，可能需要几分钟。后续启动会很快！
